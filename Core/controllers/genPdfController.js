/// import service here


exports.genPdf = (req, res, next) =>{
    // get the xlsx file from request body

    // get the json data from request

    // send to the service and get pdf 

    // return pdf with json status
    return res.status(200).json({"asdf":"Asdf"});
};
